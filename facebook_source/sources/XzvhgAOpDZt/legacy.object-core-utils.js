__d("legacy:object-core-utils", ["isEmpty", "copyProperties"]function(global, require, requireDynamic, requireLazy) {
    global.is_empty = require('isEmpty');
    global.copyProperties = require('copyProperties');
});
