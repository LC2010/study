__d("legacy:object-core-utils", ["isEmpty", "copyProperties"]function(global /*a*/ , require /*b*/ , requireDynamic /*c*/ , requireLazy /*d*/ ) {
    global /*a*/ .is_empty = require /*b*/ ('isEmpty');
    global /*a*/ .copyProperties = require /*b*/ ('copyProperties');
});
